data_filename:

domains_dict = {domain : paths_dict}
paths_dict = {path : entry_dict}
entry_dict : keys: 'title', 'time', 'domain', 'path'


record_filename:

domains_dict: = {domain : (paths_dict, time_intervals)}
paths_dict: {path : (entry_dict, time_intervals)}
entry_dict : same as above
